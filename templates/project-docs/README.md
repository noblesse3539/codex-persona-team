# Persona project document templates

`persona project init` renders these templates into a project's chosen `docs/persona/` directory. Runtime project knowledge belongs to that project, never to this Persona source repository.
