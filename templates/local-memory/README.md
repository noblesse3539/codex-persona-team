# Local memory schema examples

These files document the generic v2 schema only. The installer never writes a creator's real profile here. Runtime entries live under `~/.codex/persona-team/private/` and are deleted with the confirmed Persona uninstall.
