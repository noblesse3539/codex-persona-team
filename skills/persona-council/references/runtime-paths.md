# Runtime paths

The installer replaces this file in the installed skill with the absolute local Persona command and canonical repository paths.

From the source repository on macOS, run `scripts/persona.sh`. Persona Team v2 does not provide another operating-system runtime.
