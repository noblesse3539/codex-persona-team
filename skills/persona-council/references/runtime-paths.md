# Runtime paths

The installer replaces this file in the installed skill with the local absolute Persona command and repository paths. In the source repository, run `scripts/persona.sh` on macOS or `scripts/persona.ps1` on Windows.
